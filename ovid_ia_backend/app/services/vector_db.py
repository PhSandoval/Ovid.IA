import logging
logger = logging.getLogger(__name__)

def adicionar_documento(id_doc, texto, metadados):
    logger.warning("Mock adicionar_documento chamado (ChromaDB ausente).")

def buscar_similaridade(query, limite=3):
    logger.warning("Mock buscar_similaridade chamado (ChromaDB ausente).")
    return []
